#include <stdio.h>
int swap(int *d,int *f);

int main()
{
 int a=1,b=5;
 swap(&a,&b); 
 printf("print the elements after swapping: a = %d and b = %d\n",a,b);

    return 0;
}
int swap(int *d,int *f) 
{
 int temp;   
   temp = *d;
   *d = *f;
   *f = temp;
   return temp;
}
